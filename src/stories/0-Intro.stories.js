import React from 'react';
import { linkTo } from '@storybook/addon-links';

export default {
  title: 'Intro'
};

export const Intro = () => <div>This 
  is SPA application, where User can Add the Post and Publish the Post
  </div>;

